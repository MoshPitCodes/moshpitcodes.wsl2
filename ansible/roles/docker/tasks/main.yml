---
# Docker role - tasks
# Installs and configures Docker and Docker Compose

- name: Install prerequisites for Docker
  ansible.builtin.apt:
    name:
      - ca-certificates
      - curl
      - gnupg
      - lsb-release
    state: present
  tags: docker

- name: Create keyrings directory
  ansible.builtin.file:
    path: /etc/apt/keyrings
    state: directory
    mode: '0755'
  tags: docker

- name: Add Docker GPG key
  ansible.builtin.apt_key:
    url: https://download.docker.com/linux/ubuntu/gpg
    keyring: /etc/apt/keyrings/docker.gpg
    state: present
  tags: docker

- name: Get Ubuntu codename
  command: lsb_release -cs
  register: ubuntu_codename
  changed_when: false
  tags: docker

- name: Add Docker repository
  ansible.builtin.apt_repository:
    repo: "deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu {{ ubuntu_codename.stdout }} stable"
    state: present
    filename: docker
  tags: docker

- name: Install Docker
  ansible.builtin.apt:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-buildx-plugin
      - docker-compose-plugin
    state: present
    update_cache: true
  tags: docker

- name: Ensure Docker service is enabled
  ansible.builtin.systemd:
    name: docker
    enabled: true
    state: started
  ignore_errors: true  # WSL might not use systemd
  tags: docker

- name: Add user to docker group
  ansible.builtin.user:
    name: "{{ target_user }}"
    groups: docker
    append: true
  when: docker_add_user_to_group | default(true)
  tags: docker

- name: Create Docker daemon configuration directory
  ansible.builtin.file:
    path: /etc/docker
    state: directory
    mode: '0755'
  tags: docker

- name: Configure Docker daemon
  ansible.builtin.copy:
    content: "{{ docker_daemon_config | to_nice_json }}"
    dest: /etc/docker/daemon.json
    mode: '0644'
  when: docker_daemon_config is defined
  notify: restart docker
  tags: docker

- name: Check Docker version
  command: docker --version
  register: docker_version
  changed_when: false
  tags: docker

- name: Display Docker version
  ansible.builtin.debug:
    msg: "{{ docker_version.stdout }}"
  tags: docker

- name: Create docker-compose wrapper for compatibility
  ansible.builtin.copy:
    content: |
      #!/bin/bash
      docker compose "$@"
    dest: /usr/local/bin/docker-compose
    mode: '0755'
  tags: docker

- name: Display Docker Compose version
  command: docker compose version
  register: compose_version
  changed_when: false
  tags: docker

- name: Show Docker Compose version
  ansible.builtin.debug:
    msg: "{{ compose_version.stdout }}"
  tags: docker

- name: WSL-specific Docker configuration
  block:
    - name: Check if WSL environment
      ansible.builtin.stat:
        path: /proc/sys/fs/binfmt_misc/WSLInterop
      register: wsl_check

    - name: Create systemd override directory for Docker (WSL)
      file:
        path: /etc/systemd/system/docker.service.d
        state: directory
        mode: '0755'
      when: wsl_check.stat.exists

    - name: Configure Docker for WSL
      copy:
        content: |
          [Service]
          ExecStart=
          ExecStart=/usr/bin/dockerd -H fd:// --containerd=/run/containerd/containerd.sock
        dest: /etc/systemd/system/docker.service.d/override.conf
        mode: '0644'
      when: wsl_check.stat.exists
      notify: restart docker
  tags: docker

- name: Display post-installation message
  ansible.builtin.debug:
    msg:
      - "Docker installation complete!"
      - "Note: You may need to log out and back in for group changes to take effect"
      - "Or run: newgrp docker"
      - ""
      - "In WSL, you might need to start Docker manually:"
      - "  sudo service docker start"
  tags: docker
